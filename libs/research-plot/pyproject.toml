[project]
name = "research-plot"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "matplotlib",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
