[project]
name = "rl-components"
version = "0.1.0"
description = "Shared RL components (buffers, networks, types)"
dependencies = [
    "jax~=0.9",
    "flax~=0.12",
    "chex~=0.1",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
