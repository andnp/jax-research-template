[project]
name = "experiment-definition"
version = "0.1.0"
description = "Planned library: experiment-definition"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
