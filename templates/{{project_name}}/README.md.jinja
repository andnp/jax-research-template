# {{ project_name }}

{{ description }}

## Setup

From the workspace root:

```bash
uv sync --all-packages
```

## Run

From the workspace root:

```bash
uv run --project projects/{{ project_name }} python train.py
```
